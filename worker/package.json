{
  "name": "worker",
  "version": "1.0.0",
  "description": "Single-region health monitoring worker that performs HTTP checks on configured targets and stores metrics in Redis.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.2.3",
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "redis": "^5.9.0",
    "url": "^0.11.4"
  }
}
